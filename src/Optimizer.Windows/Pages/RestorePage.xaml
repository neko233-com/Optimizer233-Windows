<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Optimizer_Windows.Pages.RestorePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{StaticResource ShellSurfaceBrush}"
    mc:Ignorable="d">
    <Grid Padding="16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource SectionCardStyle}">
            <StackPanel Spacing="12">
                <TextBlock x:Name="PageTitleText" FontSize="22" FontFamily="Bahnschrift" FontWeight="SemiBold" Text="Restore" />
                <TextBlock x:Name="PageSubtitleText" Foreground="{StaticResource ShellTextMutedBrush}" TextWrapping="Wrap" Text="Export, import, and reset Optimizer233-Windows configuration." />
                <TextBlock x:Name="StatusText" Foreground="{StaticResource ShellTextMutedBrush}" TextWrapping="Wrap" />
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" ColumnSpacing="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button x:Name="ExportButton" Grid.Column="0" Click="ExportButton_Click" Style="{StaticResource SecondaryActionButtonStyle}" Content="Export Config" />
            <Button x:Name="ImportButton" Grid.Column="1" Click="ImportButton_Click" Style="{StaticResource SecondaryActionButtonStyle}" Content="Import Config" />
            <Button x:Name="ResetButton" Grid.Column="2" Click="ResetButton_Click" Style="{StaticResource SecondaryActionButtonStyle}" Content="Reset Config" />
            <Button x:Name="OpenSystemRestoreButton" Grid.Column="3" Click="OpenSystemRestoreButton_Click" Style="{StaticResource PrimaryActionButtonStyle}" Content="Open System Restore" />
        </Grid>
    </Grid>
</Page>
