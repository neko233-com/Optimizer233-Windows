<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Optimizer_Windows.Pages.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{StaticResource ShellSurfaceBrush}"
    Loaded="Page_Loaded"
    mc:Ignorable="d">
    <Grid Padding="20" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource HeroPanelStyle}">
            <Grid ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="340" />
                </Grid.ColumnDefinitions>

                <StackPanel Spacing="8">
                    <Border Style="{StaticResource TechChipStyle}" HorizontalAlignment="Left">
                        <TextBlock x:Name="HeroEyebrowText" FontFamily="Bahnschrift" Foreground="{StaticResource ShellAccentSecondaryBrush}" />
                    </Border>
                    <TextBlock x:Name="WelcomeTitleText" FontSize="28" FontFamily="Bahnschrift" FontWeight="SemiBold" />
                    <TextBlock x:Name="WelcomeSubtitleText" Foreground="{StaticResource ShellTextMutedBrush}" TextWrapping="Wrap" />
                    <TextBlock x:Name="SnapshotText" Foreground="{StaticResource ShellTextFaintBrush}" TextWrapping="Wrap" />
                </StackPanel>

                <Border Grid.Column="1" Style="{StaticResource SectionCardStyle}" Padding="16">
                    <StackPanel Spacing="10">
                        <TextBlock x:Name="RecommendationTitleText" FontSize="18" FontFamily="Bahnschrift" FontWeight="SemiBold" />
                        <TextBlock x:Name="RecommendationBodyText" Foreground="{StaticResource ShellTextMutedBrush}" TextWrapping="Wrap" />
                        <Button x:Name="RefreshButton" Click="RefreshButton_Click" HorizontalAlignment="Left" Style="{StaticResource PrimaryActionButtonStyle}" />
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <Grid Grid.Row="1" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource SectionCardStyle}" Padding="16">
                <StackPanel Spacing="6">
                    <TextBlock x:Name="DeviceLabelText" Foreground="{StaticResource ShellTextMutedBrush}" FontFamily="Bahnschrift" />
                    <TextBlock x:Name="DeviceValueText" FontSize="18" FontFamily="Bahnschrift" FontWeight="SemiBold" TextWrapping="Wrap" />
                    <TextBlock x:Name="DeviceHintText" Foreground="{StaticResource ShellTextFaintBrush}" TextWrapping="Wrap" />
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource SectionCardStyle}" Padding="16">
                <StackPanel Spacing="6">
                    <TextBlock x:Name="WindowsLabelText" Foreground="{StaticResource ShellTextMutedBrush}" FontFamily="Bahnschrift" />
                    <TextBlock x:Name="WindowsValueText" FontSize="18" FontFamily="Bahnschrift" FontWeight="SemiBold" TextWrapping="Wrap" />
                    <TextBlock x:Name="WindowsHintText" Foreground="{StaticResource ShellTextFaintBrush}" TextWrapping="Wrap" />
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource SectionCardStyle}" Padding="16">
                <StackPanel Spacing="6">
                    <TextBlock x:Name="MemoryLabelText" Foreground="{StaticResource ShellTextMutedBrush}" FontFamily="Bahnschrift" />
                    <TextBlock x:Name="MemoryValueText" FontSize="18" FontFamily="Bahnschrift" FontWeight="SemiBold" TextWrapping="Wrap" />
                    <ProgressBar x:Name="MemoryPressureBar" Minimum="0" Maximum="100" Height="6" />
                    <TextBlock x:Name="MemoryHintText" Foreground="{StaticResource ShellTextFaintBrush}" TextWrapping="Wrap" />
                </StackPanel>
            </Border>

            <Border Grid.Column="3" Style="{StaticResource SectionCardStyle}" Padding="16">
                <StackPanel Spacing="6">
                    <TextBlock x:Name="StorageLabelText" Foreground="{StaticResource ShellTextMutedBrush}" FontFamily="Bahnschrift" />
                    <TextBlock x:Name="StorageValueText" FontSize="18" FontFamily="Bahnschrift" FontWeight="SemiBold" TextWrapping="Wrap" />
                    <ProgressBar x:Name="StoragePressureBar" Minimum="0" Maximum="100" Height="6" />
                    <TextBlock x:Name="StorageHintText" Foreground="{StaticResource ShellTextFaintBrush}" TextWrapping="Wrap" />
                </StackPanel>
            </Border>

            <Border Grid.Column="4" Style="{StaticResource SectionCardStyle}" Padding="16">
                <StackPanel Spacing="6">
                    <TextBlock x:Name="SecurityLabelText" Foreground="{StaticResource ShellTextMutedBrush}" FontFamily="Bahnschrift" />
                    <TextBlock x:Name="SecurityValueText" FontSize="18" FontFamily="Bahnschrift" FontWeight="SemiBold" TextWrapping="Wrap" />
                    <TextBlock x:Name="SecurityHintText" Foreground="{StaticResource ShellTextFaintBrush}" TextWrapping="Wrap" />
                </StackPanel>
            </Border>
        </Grid>

        <Grid Grid.Row="2" ColumnSpacing="14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.1*" />
                <ColumnDefinition Width="0.9*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource SectionCardStyle}" Padding="18">
                <Grid RowSpacing="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock x:Name="HealthTitleText" FontSize="20" FontFamily="Bahnschrift" FontWeight="SemiBold" />
                    <TextBlock x:Name="BootText" Grid.Row="1" Foreground="{StaticResource ShellTextMutedBrush}" TextWrapping="Wrap" />
                    <TextBlock x:Name="NetworkText" Grid.Row="2" Foreground="{StaticResource ShellTextMutedBrush}" TextWrapping="Wrap" />
                    <TextBlock x:Name="DashboardHintText" Grid.Row="3" Foreground="{StaticResource ShellTextFaintBrush}" TextWrapping="Wrap" />
                    <Grid Grid.Row="4" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Style="{StaticResource InsetPanelStyle}" Padding="14">
                            <StackPanel Spacing="6">
                                <TextBlock x:Name="CpuHealthLabelText" Foreground="{StaticResource ShellTextMutedBrush}" FontFamily="Bahnschrift" />
                                <TextBlock x:Name="CpuHealthValueText" FontSize="18" FontFamily="Bahnschrift" FontWeight="SemiBold" TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="1" Style="{StaticResource InsetPanelStyle}" Padding="14">
                            <StackPanel Spacing="6">
                                <TextBlock x:Name="MemoryHealthLabelText" Foreground="{StaticResource ShellTextMutedBrush}" FontFamily="Bahnschrift" />
                                <TextBlock x:Name="MemoryHealthValueText" FontSize="18" FontFamily="Bahnschrift" FontWeight="SemiBold" TextWrapping="Wrap" />
                                <ProgressBar x:Name="MemoryHealthBar" Minimum="0" Maximum="100" Height="6" />
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="2" Style="{StaticResource InsetPanelStyle}" Padding="14">
                            <StackPanel Spacing="6">
                                <TextBlock x:Name="StorageHealthLabelText" Foreground="{StaticResource ShellTextMutedBrush}" FontFamily="Bahnschrift" />
                                <TextBlock x:Name="StorageHealthValueText" FontSize="18" FontFamily="Bahnschrift" FontWeight="SemiBold" TextWrapping="Wrap" />
                                <ProgressBar x:Name="StorageHealthBar" Minimum="0" Maximum="100" Height="6" />
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="3" Style="{StaticResource InsetPanelStyle}" Padding="14">
                            <StackPanel Spacing="6">
                                <TextBlock x:Name="ConfigScoreLabelText" Foreground="{StaticResource ShellTextMutedBrush}" FontFamily="Bahnschrift" />
                                <TextBlock x:Name="ConfigScoreValueText" FontSize="18" FontFamily="Bahnschrift" FontWeight="SemiBold" TextWrapping="Wrap" />
                                <TextBlock x:Name="ConfigScoreHintText" Foreground="{StaticResource ShellTextFaintBrush}" TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>
                    </Grid>
                </Grid>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource SectionCardStyle}" Padding="18">
                <StackPanel Spacing="12">
                    <TextBlock x:Name="ActionsTitleText" FontSize="20" FontFamily="Bahnschrift" FontWeight="SemiBold" />
                    <TextBlock x:Name="ActionsHintText" Foreground="{StaticResource ShellTextFaintBrush}" TextWrapping="Wrap" />
                    <Grid ColumnSpacing="10" RowSpacing="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Button x:Name="OpenCleanupButton" Grid.Row="0" Grid.Column="0" Click="OpenCleanupButton_Click" Style="{StaticResource SecondaryActionButtonStyle}" />
                        <Button x:Name="OpenSecurityButton" Grid.Row="0" Grid.Column="1" Click="OpenSecurityButton_Click" Style="{StaticResource SecondaryActionButtonStyle}" />
                        <Button x:Name="OpenUpdatesButton" Grid.Row="1" Grid.Column="0" Click="OpenUpdatesButton_Click" Style="{StaticResource SecondaryActionButtonStyle}" />
                        <Button x:Name="OpenHardwareButton" Grid.Row="1" Grid.Column="1" Click="OpenHardwareButton_Click" Style="{StaticResource SecondaryActionButtonStyle}" />
                        <Button x:Name="OpenStorageSenseButton" Grid.Row="2" Grid.Column="0" Click="OpenStorageSenseButton_Click" Style="{StaticResource SecondaryActionButtonStyle}" />
                        <Button x:Name="OpenTaskManagerButton" Grid.Row="2" Grid.Column="1" Click="OpenTaskManagerButton_Click" Style="{StaticResource SecondaryActionButtonStyle}" />
                        <Button x:Name="OpenWindowsUpdateButton" Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Click="OpenWindowsUpdateButton_Click" Style="{StaticResource PrimaryActionButtonStyle}" />
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>
